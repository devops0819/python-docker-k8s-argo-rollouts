apiVersion: v1
kind: Service
metadata:
  name: argo-rollouts-dashboard
  namespace: argocd
spec:
  ports:
  - port: 3100
    targetPort: 3100
    protocol: TCP
    name: http
  selector:
    app: rollouts-dashboard
  type: LoadBalancer
